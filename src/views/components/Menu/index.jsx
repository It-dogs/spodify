import React, { useState, useEffect, useRef } from "react";
import NavBar from '../NavBar';

const Menu = (props) => {
    return (
      <div>
        <NavBar />
      </div>
    );
}

export default Menu;